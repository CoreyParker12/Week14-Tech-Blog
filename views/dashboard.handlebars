<div>
  <div>
    <p>
      You have reached the dashboard, {{name}}. Here you can see all of your posts.
    </p>
    <div>
      {{#each posts as |post|}}
      {{> posts-display}}
      {{/each}}
    </div>
  </div>
</div>